name: chicachew
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.7          # 라우팅
  shared_preferences: ^2.3.2  # 로컬 저장(프로필/설정 등)
  cupertino_icons: ^1.0.8     # (아이콘 세트)
  camera: ^0.11.2
  permission_handler: ^12.0.1
  # google_mlkit_face_detection: ^0.13.1
  # google_mlkit_face_mesh_detection: ^0.4.1
  # google_mlkit_commons: ^0.11.0
  flutter_tts: ^3.8.3
  tflite_flutter: 0.11.0
  # brush_runtime: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/          # 인트로/캐릭터 이미지 폴더
    - assets/models/
    - assets/brush_zone.txt
    - assets/mp/face_landmarker.task
    - assets/mp/hand_landmarker.task